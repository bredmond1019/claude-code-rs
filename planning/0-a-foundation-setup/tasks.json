[
  { "task_id": 1, "title": "Initialize crate + lean deps + .gitignore", "description": "- Run `cargo init --lib` (or hand-author `Cargo.toml` + `src/lib.rs` placeholder) so the crate builds.\n- In `Cargo.toml`: set package name `claude-code-rs`, edition 2021, and pin ONLY the lean dep set — `tokio` with features `[\"process\", \"rt\", \"macros\", \"time\"]` (NOT `full`), `serde` (feature `derive`), `serde_json`, `thiserror`, `which`.\n- Do NOT add `async-trait`, `sqlx`, or any telemetry deps.\n- Write `.gitignore` ignoring `/target` (and `Cargo.lock` only if this stays a library — keep `/target` at minimum).", "acceptance_criteria": [], "validation_commands": [], "max_attempts": 3, "files": ["Cargo.toml", ".gitignore"], "dependsOn": [] },
  { "task_id": 2, "title": "Crate-level Error + Result in src/error.rs", "description": "- Create `src/error.rs` defining a crate-level `Error` enum deriving `thiserror::Error` and `Debug`.\n- Seed it with a small forward-looking variant set the downstream blocks will use (e.g. `BinaryNotFound`, `Spawn(std::io::Error)`, `Timeout`, `Parse(serde_json::Error)`) — enough to compile warning-free without over-building.\n- Define `pub type Result<T> = std::result::Result<T, Error>;`.\n- Keep it stub-light: no execution/parse logic, just the error surface.", "acceptance_criteria": [], "validation_commands": [], "max_attempts": 3, "files": ["src/error.rs"], "dependsOn": [1] },
  { "task_id": 3, "title": "Module skeleton + re-exports in src/lib.rs", "description": "- In `src/lib.rs`, declare the module skeleton: `mod error;` plus empty inline stub modules `pub mod config {}`, `pub mod execute {}`, `pub mod parse {}`, `pub mod isolation {}` (inline empty blocks keep the block's file list to lib.rs + error.rs and stay warning-free).\n- Re-export the crate-level error surface: `pub use error::{Error, Result};`.\n- Add a crate-level `//!` doc comment describing the SDK in one line.\n- Add one trivial unit test (e.g. asserting `Result<()>` composes / a smoke `#[test]`) so the crate satisfies standing rule 1 and `cargo test` has something to run.\n- Ensure `cargo build`, `cargo clippy -- -D warnings`, and `cargo fmt --check` are all clean.", "acceptance_criteria": [], "validation_commands": [], "max_attempts": 3, "files": ["src/lib.rs"], "dependsOn": [1, 2] },
  { "task_id": 4, "title": "Fill harness.json (Rust profile) + sync CLAUDE.md commands", "description": "- Replace the `REPLACE-ME` stub in `planning/harness.json` with the Rust profile from `planning/harness.examples.md`: `stack: \"rust\"`; `validation.checks[]` = fmt (`cargo fmt --check`), clippy (`cargo clippy -- -D warnings`), test (`cargo test`), build (`cargo build --release`), each `gates: true`; `uiTest.enabled: false`. Preserve the existing `breakdown`/`planning`/`block`/`flow` sections.\n- Update `CLAUDE.md`'s *Build / test / run* code block so its `<build>`/`<test>`/`<run>` commands match those checks exactly (per the block acceptance criterion and standing rule that the two stay in sync).\n- This task owns disjoint files from tasks 1–3, so it can run in parallel.", "acceptance_criteria": [], "validation_commands": [], "max_attempts": 3, "files": ["planning/harness.json", "CLAUDE.md"], "dependsOn": [] },
  { "task_id": 5, "title": "Validate", "description": "Run the Validation Commands listed in tasks.md and confirm all pass: `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test`, `cargo build --release` — all clean, no warnings.", "acceptance_criteria": [], "validation_commands": [], "max_attempts": 3, "files": [], "dependsOn": [1, 2, 3, 4] }
]
